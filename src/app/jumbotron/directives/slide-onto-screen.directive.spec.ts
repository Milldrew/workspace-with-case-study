import { SlideOntoScreenDirective } from './slide-onto-screen.directive';

describe('SlideOntoScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideOntoScreenDirective();
    expect(directive).toBeTruthy();
  });
});

import { MaximizeCardDirective } from './maximize-card.directive';

describe('MaximizeCardDirective', () => {
  it('should create an instance', () => {
    const directive = new MaximizeCardDirective();
    expect(directive).toBeTruthy();
  });
});
